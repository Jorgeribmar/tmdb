type Movie {
  id: ID!
  originalTitle: String
  overview: String
  releaseDate: String
  voteAverage: String
  posterPath: String
}

type Query {
  movies(query: String!): [Movie]!
  movie(id: ID!): Movie
}
